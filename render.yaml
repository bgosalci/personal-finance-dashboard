services:
  - type: web
    name: personal-finance-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app
    plan: free
    healthCheckPath: /api/options?ticker=SPY
